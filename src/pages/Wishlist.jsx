import React from "react";

const Wishlist = () => (
  <main>
    <section className="hero">
      <h1>Your Wishlist</h1>
      <p>Products you love, all in one place!</p>
    </section>
    <section className="products">
      <h2>Saved Products</h2>
      {/* Add wishlist product components or list here */}
    </section>
  </main>
);

export default Wishlist;
